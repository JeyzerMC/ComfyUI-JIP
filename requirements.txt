Pillow
numpy
# Background removal (B5) — permissive backend, pinned in that issue once chosen.
# rembg
# Controlnet preprocessing (B4) is provided by the separately-installed
# comfyui_controlnet_aux extension (Apache-2.0); not vendored here.
